import { StyleSheet } from 'react-native';

export const rootStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    title: {
        fontSize: 30,
        textAlign: 'center'
    },
    text: {
        fontSize: 20,
        textAlign: 'center'
    }
});
